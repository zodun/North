node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# Firebase Cloud Messaging config files (DEC-21). Per-environment;
# operator places GoogleService-Info.plist + google-services.json here
# for local dev. EAS builds receive them via `eas secret:create --type
# file` per profile. Never committed.
google-services/

